import 'package:flutter/material.dart';
import 'package:gogo_password/view/common.dart';

class EditLoginInfoPage extends StatelessWidget {
  const EditLoginInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: BaseAppBar(title: 'title'),
      body: const Center(),
    );
  }
}
