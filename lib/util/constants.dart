enum BankType { bankAccount, creditCard, unKnown }

enum CreditCardType { visa, masterCard, other, unKnown }

enum ContentEditState { onlyRead, editing }

enum LocalSecureInfoType { login, bank, note, unKnown }
